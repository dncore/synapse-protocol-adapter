# synapse systemd unit.
#
# Install:
#   sudo cp deploy/systemd/synapse.service /etc/systemd/system/
#   sudo systemctl daemon-reload
#   sudo systemctl enable --now synapse
#
# The binary is expected at /usr/local/bin/synapse and the config
# at /etc/synapse/config.yaml (see ExecStart; adjust to taste).

[Unit]
Description=Responses -> Completions protocol proxy (synapse-protocol-adapter)
Documentation=https://github.com/dncore/synapse-protocol-adapter
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=synapse
Group=synapse
# Hardening: no new privileges, isolated temp/home.
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
PrivateTmp=true
ReadWritePaths=

ExecStart=/usr/local/bin/synapse --config /etc/synapse/config.yaml
ExecReload=/bin/kill -HUP $MAINPID

# graceful shutdown: SIGTERM drains in-flight streams up to
# shutdown.timeout; TimeoutStopSec must exceed it.
KillSignal=SIGTERM
TimeoutStopSec=35s

# Restart on unexpected exit; drain failures exit cleanly (0).
Restart=on-failure
RestartSec=2s

# Logs go to journald (structured JSON lines; view with journalctl -u synapse).
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
